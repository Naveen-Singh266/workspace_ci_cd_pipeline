<<<<<<< HEAD
name: DevOps CI-CD Pipeline
=======
name: workspace_ci_cd_pipeline
>>>>>>> 141c161a10703845401c7cd94ef3d4fe899bca60

on:
  push:
    branches: [ master ]

jobs:

  build:
    runs-on: self-hosted

    steps:

    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Docker Login
      run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
    
    - name: Build Docker Image
      run: docker build -t naveen266/devops-app:${{ github.run_number }} .

    - name: Push Docker Image
      run: docker push naveen266/devops-app:${{ github.run_number }}

  deploy:
    needs: build
    runs-on: self-hosted

    steps:

    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Terraform Init
      working-directory: infra
      run: terraform init

    - name: terraform validate
      working-directory: infra
      run: terraform validate

    - name: Terraform Plan
      working-directory: infra
      run: terraform plan -var="image_tag=${{ github.run_number }}"

    - name: Terraform Apply
      working-directory: infra
      run: terraform apply -auto-approve -var="image_tag=${{ github.run_number }}"

    - name: Verify Deployment  
<<<<<<< HEAD
      run: kubectl get pods -o wide
=======
      run: kubectl get pods -0 wide
>>>>>>> 141c161a10703845401c7cd94ef3d4fe899bca60
